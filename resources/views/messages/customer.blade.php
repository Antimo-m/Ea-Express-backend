<x-guest-layout title="Parla con EA-Express"><span class="eyebrow">ASSISTENZA ALLA SPEDIZIONE</span><h1 class="h3">Parla con EA-Express</h1><p class="small text-secondary text-break">{{ $reference }}</p>@if($errors->any())<div class="alert alert-danger" role="alert">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('conversation.store', $token) }}" class="form-stack mb-4">@csrf<label for="body" class="form-label">Il tuo messaggio</label><textarea id="body" name="body" class="form-control" rows="3" maxlength="2000" required>{{ old('body') }}</textarea><button class="btn btn-primary">Invia al rider <x-ui.icon name="send" /></button></form><h2 class="h5">Conversazione</h2><x-orders.messages :messages="$messages" :customer="true" /><p class="small text-secondary mt-4">Questo link è riservato. Ricarica la pagina per leggere le risposte del rider.</p></x-guest-layout>
