<x-app-layout title="Notifiche"><header class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-4"><div><span class="eyebrow">AGGIORNAMENTI</span><h1>Notifiche</h1></div><form method="post" action="{{ route('notifications.read-all') }}">@csrf @method('patch')<button class="btn btn-outline-primary">Segna tutte come lette</button></form></header>@forelse($notifications as $notification)<article class="surface p-4 mb-3"><div class="d-flex justify-content-between flex-wrap gap-3"><div><h2 class="h5">{{ $notification->data['title'] }}</h2><p class="text-secondary small text-break mb-1">{{ $notification->data['reference'] }} · {{ $notification->created_at->timezone('Europe/Rome')->format('d/m/Y H:i') }}</p>@unless($notification->read_at)<span class="status-pill tone-orange">Da leggere</span>@endunless</div><form method="post" action="{{ route('notifications.update', $notification->id) }}">@csrf @method('patch')<button class="btn btn-outline-primary">Apri aggiornamento</button></form></div></article>@empty<div class="surface p-5 text-center"><h2 class="h5">Nessuna notifica</h2><p class="text-secondary mb-0">Qui troverai aggiornamenti sugli ordini e nuovi messaggi.</p></div>@endforelse{{ $notifications->links() }}</x-app-layout>
