<x-app-layout title="Messaggi"><header class="mb-4"><span class="eyebrow">IN CONTATTO</span><h1>Messaggi</h1><p class="text-secondary">Conversazioni con i clienti, collegate alle spedizioni.</p></header>@forelse($orders as $order)<article class="surface p-4 mb-3"><div class="d-flex justify-content-between gap-3 flex-wrap"><div><h2 class="h5"><a href="{{ route('messages.show', $order) }}">{{ $order->store_name }}</a></h2><p class="small text-secondary text-break mb-1">{{ $order->reference }}</p><span class="small">{{ $order->status->label() }}</span></div><div>@if($order->unread_count)<span class="status-pill tone-orange">{{ $order->unread_count }} da leggere dal team</span>@endif<a class="btn btn-outline-primary ms-2" href="{{ route('messages.show', $order) }}">Apri</a></div></div></article>@empty<div class="surface p-5 text-center"><h2 class="h5">Nessuna conversazione</h2><p class="text-secondary">Apri un ordine per iniziare a scrivere al cliente.</p><a class="btn btn-primary" href="{{ route('orders.incoming') }}">Vai agli ordini</a></div>@endforelse{{ $orders->links() }}</x-app-layout>
