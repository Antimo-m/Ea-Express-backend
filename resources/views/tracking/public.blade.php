<x-guest-layout title="Tracking spedizione"><span class="eyebrow">LA TUA SPEDIZIONE</span><h1 class="h3 text-break">{{ $reference }}</h1><span class="status-pill tone-{{ $status->tone() }} mb-4">{{ $status->label() }}</span>@if($estimated && ! in_array($status->value, \App\OrderStatus::closed(), true))<p>Previsione: <strong>{{ $estimated->timezone('Europe/Rome')->format('d/m/Y H:i') }}</strong><br><small class="text-secondary">Orario indicativo, aggiornato dal rider.</small></p>@endif<h2 class="h5">Percorso della spedizione</h2><ol class="tracking-timeline mt-3">@foreach($events as $event)<li><strong>{{ $event->status->label() }}</strong><div class="small text-secondary">{{ $event->created_at->timezone('Europe/Rome')->format('d/m/Y H:i') }}</div>@if($event->public_note)<p class="text-break">{{ $event->public_note }}</p>@endif</li>@endforeach</ol>{{ $events->links() }}<p class="small text-secondary">Aggiornamenti manuali del rider. Ricarica la pagina per vedere le novità.</p></x-guest-layout>
