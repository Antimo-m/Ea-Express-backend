@props(['order'])
<article class="request-card" aria-labelledby="request-{{ $order['id'] }}">
    <div class="d-flex justify-content-between align-items-start gap-2"><div class="d-flex gap-3 align-items-center min-width-0"><span class="shop-avatar">{{ $order['initials'] }}</span><div class="min-width-0"><h3 class="h6 mb-1" id="request-{{ $order['id'] }}">{{ $order['shop'] }}</h3><span class="small text-secondary">#{{ $order['id'] }}</span></div></div><span @class(['status-pill', 'tone-orange' => $order['urgent'], 'tone-neutral' => ! $order['urgent']])><x-ui.icon :name="$order['urgent'] ? 'lightning-charge' : 'clock'" />{{ $order['urgent'] ? 'Urgente' : 'Standard' }}</span></div>
    <div class="request-route"><div><span class="route-dot"></span><div><small>RITIRO</small><strong>{{ $order['from'] }}</strong></div></div><div><span class="route-dot destination"></span><div><small>CONSEGNA</small><strong>{{ $order['to'] }}</strong></div></div></div>
    <div class="request-meta"><span><x-ui.icon name="clock" />Oggi, {{ $order['window'] }}</span><span><x-ui.icon name="box-seam" />{{ $order['parcels'] }} {{ $order['parcels'] === 1 ? 'pacco' : 'pacchi' }}</span></div>
    <details class="request-details"><summary>Dettagli della richiesta</summary><dl class="small mb-0"><dt>Cliente</dt><dd>{{ $order['customer'] }}</dd><dt>Indirizzo di ritiro</dt><dd>{{ $order['pickup'] }}</dd><dt>Indirizzo di consegna</dt><dd>{{ $order['delivery'] }}</dd><dt>Fascia di consegna</dt><dd>{{ $order['delivery_window'] }}</dd><dt>Categoria</dt><dd>{{ $order['category'] }}</dd><dt>Note per il rider</dt><dd>{{ $order['note'] }}</dd></dl></details>
    <div class="request-actions"><button type="button" class="btn btn-success" disabled aria-describedby="demo-actions-note"><x-ui.icon name="check-circle" class="me-2" />Accetta</button><button type="button" class="btn btn-outline-danger" disabled aria-describedby="demo-actions-note"><x-ui.icon name="x-circle" class="me-2" />Rifiuta</button></div>
</article>
