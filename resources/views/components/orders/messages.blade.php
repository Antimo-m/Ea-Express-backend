@props(['messages', 'customer' => false])
<div class="conversation-list" data-conversation-list>@forelse($messages->getCollection()->reverse() as $message)<article class="message-bubble {{ $message->user_id ? 'message-team' : '' }}"><div class="d-flex justify-content-between gap-3 flex-wrap small"><strong>{{ $message->user_id ? 'EA-Express' : 'Cliente' }}</strong><span class="text-secondary">{{ $message->created_at->timezone('Europe/Rome')->format('d/m/Y H:i') }}</span></div><p class="message-body mb-1 mt-2">{{ $message->body }}</p>@if(!$customer && !$message->user_id)<span class="small text-secondary">{{ $message->read_at ? 'Letto dal team' : 'Da leggere' }}</span>@endif</article>@empty<p class="text-secondary">La conversazione è pronta. Scrivi il primo messaggio.</p>@endforelse</div><div class="mt-3">{{ $messages->links() }}</div>
